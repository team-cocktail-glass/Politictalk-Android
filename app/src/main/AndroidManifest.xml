<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zingdo.myapplication">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FindPassword" />
        <!--<activity android:name=".Send_email" />-->
        <activity android:name=".MainBill" />
        <activity android:name=".SendEmail" />
        <activity android:name=".Home" />
        <activity android:name=".HomeAlarm" />
        <activity android:name=".FilterChoice" />
        <activity android:name=".BillWrite" />
        <activity android:name=".ShowJoiner" />
        <activity android:name=".ShowPosts" />
        <activity android:name=".BillSearch" />
        <activity android:name=".MyPage" />
    </application>

</manifest>